# Tile toggle — expose/tile all windows as panes and back
# Source this from tmux.conf or paste into it to re-enable:
#   bind E run-shell "$HOME/bin/tmux-tile-session toggle"

# prefix + E = expose/tile toggle (all windows ↔ tiled panes)
bind E run-shell "$HOME/bin/tmux-tile-session toggle"
